---
name: gost-lab-report
description: >
  Оформление отчётов по лабораторным работам по ГОСТ 7.32.
  Используй когда: отчёт, лабораторная, ГОСТ, оформить отчёт,
  лабораторная работа, оформление, lab report.
---

# Оформление отчёта по лабораторной работе (ГОСТ 7.32)

## Обзор

Скилл автоматизирует оформление отчётов: LLM генерирует Markdown, скрипт собирает .docx с правильными стилями и титульным листом.

## Пререквизиты

- `pandoc` (>= 3.1.12)
- Python-пакеты: `python-docx`, `docxcompose`
- Шрифт Times New Roman

## YAML-метаданные

Каждый отчёт начинается с YAML front matter:

```yaml
---
title: "Лабораторная работа №N"
teacher_title: "должность, уч. степень, звание"
teacher_name: "И.И. Фамилия"
lab_number: "N"
lab_title: "Название лабораторной работы"
discipline: "Название дисциплины"
group: "Номер группы"
student_name: "И.И. Фамилия"
department: "Название кафедры"
---
```

**Важно:** поле `subtitle` не использовать — стиль Subtitle не настроен в шаблоне и даст непредсказуемое форматирование.

## Входной контекст

LLM может получить от пользователя:
- Методичку / задание на лабораторную (PDF, текст, скриншоты)
- Скриншоты выполнения работы
- Устные указания
- Или минимум контекста (только тема)

**Правила:**
- Если есть методичка — цель работы берётся оттуда дословно, структура разделов определяется заданием
- Если методички нет — LLM формулирует цель самостоятельно на основе темы, использует типовую структуру
- Если номер варианта неясен из контекста — запросить у пользователя

## Структура отчёта

Структура гибкая и зависит от задания. Типовые разделы (порядок примерный):

| Раздел | Когда нужен |
|---|---|
| **Цель работы** | Почти всегда. Одно-два предложения |
| **Вариант** | Если есть варианты заданий — просто число (например, «Вариант 5») |
| **Краткие теоретические сведения** | По требованию методички или пользователя. Не обязателен |
| **Ход работы** | Обязательно. Описание действий + скриншоты |
| **Тестирование** | Когда есть программная реализация — скриншоты работы приложения |
| **Выводы** | Обязательно. Перекличка с целью |

Конкретные разделы могут отличаться от типовых — ориентируйся на задание и контекст.

### Цель работы

Формат: `**Цель работы:** текст цели.` — жирная метка, текст в одну-две строки.

Если задана в методичке — брать дословно. Если нет — формулировать кратко и конкретно.

### Ход работы

- Описание действия → ссылка на группу рисунков: «Выполнение представлено на рисунках N-M»
- Каждый логический блок действий — отдельный абзац
- Скриншоты группируются по смыслу, а не описываются по одному

### Выводы

- Должны подтверждать достижение цели работы
- Упоминать конкретные результаты (что настроено, реализовано, проверено)
- Перекликаться с формулировкой цели по смыслу (не дословное копирование)

### Пример структуры

```markdown
**Цель работы:** настроить единую систему аутентификации на базе контроллера домена.

# Краткие теоретические сведения

Текст...

# Ход работы

Описание действия. Выполнение представлено на рисунках 1-3.

![Рисунок 1 — Подпись](images/01_описание.png){width=80%}

: Таблица 1 — Название таблицы

| Заголовок 1 | Заголовок 2 |
|:-----------:|:-----------:|
| значение    | значение    |

\newpage

# Выводы

В ходе выполнения лабораторной работы была настроена единая система
аутентификации... что соответствует поставленной цели.
```

## Конвенции Markdown

### Заголовки

- `#` — разделы верхнего уровня
- `##` — подразделы
- `###` — пункты внутри подразделов
- Заголовки пишутся **без номеров** — нумерация отключена в шаблоне
- Капитализация: только первая буква с заглавной

### Таблицы

- Подпись таблицы **перед** таблицей в формате: `: Таблица N — Название`
- Используй pipe tables с выравниванием через `:`
- Для сложных таблиц с объединением ячеек — grid tables (ограниченная поддержка merge) или ручная правка в docx

### Изображения

- Формат: `![Рисунок N — Подпись](images/NN_описание.png){width=80%}`
- Перед рисунком — описательный абзац
- Файлы изображений в папке `images/`, именование: `NN_краткое-описание.png`
- Если есть файлы изображений — прочитай их через Read для генерации точных описаний

### Разрывы страниц

- `\newpage` — разрыв страницы (обрабатывается Lua-фильтром)
- Обязателен перед разделом «Выводы»

### Текст

- Обычные абзацы — просто текст, без специальной разметки
- Нумерованные списки: `1.`, `2.`, `3.`
- Маркированные списки: `-`

### Листинги кода

- Используй fenced code blocks с указанием языка: ` ```python ... ``` `
- В docx применяется стиль Source Code (моноширинный шрифт)
- Для лабораторных по программированию — вставляй ключевые фрагменты кода

## Продвинутые возможности

**Custom styles** — применение произвольного стиля из reference.docx:
```markdown
::: {custom-style="НазваниеСтиля"}
Текст с кастомным стилем
:::
```

**Grid tables** — для таблиц с объединением ячеек (частичная поддержка):
```markdown
+---------------+---------------+
| Объединённая ячейка           |
+---------------+---------------+
| Ячейка 1      | Ячейка 2      |
+---------------+---------------+
```

**Несколько входных файлов** — для больших отчётов:
```bash
pandoc intro.md methods.md results.md -o body.docx --reference-doc=reference.docx
```

## Сборка

```bash
python ~/.claude/skills/gost-lab-report/scripts/build.py report.md
```

Результат: `report.docx` в той же директории.

## Известные ограничения (ручная доводка)

- Центрирование рисунков может потребовать ручной правки
- Серая заливка таблиц — убрать вручную (выделить таблицу → убрать заливку)
- Подписи к таблицам и рисункам: 14pt вместо 12pt по ГОСТ — поправить вручную при необходимости
- Сложные таблицы (объединение ячеек) не поддерживаются в pipe tables — используй grid tables или правь в docx
